{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Vite Dev Server (127.0.0.1:5173)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev -- --host --port 5173",
      "cwd": "${workspaceFolder}"
    }
  ]
}
