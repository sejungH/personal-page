{
  "name": "personal-page",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:hosting": "npm run build && npx firebase-tools deploy --only hosting"
  },
  "dependencies": {
    "dompurify": "^3.4.7",
    "firebase": "^11.1.0",
    "quill": "^2.0.3"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
